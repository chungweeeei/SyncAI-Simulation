map_server:
  ros__parameters:
    use_sim_time: true
    yaml_filename: "/home/ubuntu/map/testmap.yaml"

map_saver:
  ros__parameters:
    use_sim_time: true
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: true
