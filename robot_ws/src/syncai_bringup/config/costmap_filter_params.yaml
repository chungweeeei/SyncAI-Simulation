filter_mask_server:
  ros__parameters:
    use_sim_time: true
    yaml_filename: "/home/ubuntu/map/warehouse_keepout_mask.yaml"

costmap_filter_info_server:
  ros__parameters:
    use_sim_time: true
    type: 0
    filter_info_topic: "costmap_filter_info"
    mask_topic: "/<robot_namespace>/filter_mask"
    base: 0.0
    multiplier: 1.0
